{
    // these arguments will be passed to the Haxe completion server
    // they should only contain arguments or hxml files that
    // are needed for completion, such as -cp, -lib, target output settings and defines
    "haxe.displayArguments": ["build.hxml"]
}
